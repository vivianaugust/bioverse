[pytest]
# Look for test files with names that start with "test_" or end with "_test.py"
# The default is `test_*.py`
python_files = test_*.py

# Tell pytest where to find your test directories.
# This ensures pytest discovers your tests, regardless of where you run the command from.
testpaths =
    bioverse/tests